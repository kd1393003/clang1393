#include<stdio.h>
main()
{
	char no;
	printf("演算子を入れて");
	scanf("%c", &no);
	if (no < 50 && no >= 10) {
		switch (no)
		{
		case 'a', 'A':if (k < g);
		{
			if (k < h) {
				printf("最大値%d\n",h );
			}
			else
			{
				printf("最大値%d\n",k );
			}
		}
	    else
		{
			if (g<h)
			{
				printf("最大値%d\n", h);
			}
			else
			{
				printf("最大値%d\n", g);
			}
		}
			break;
		case 'S','s':
			printf("20点台です\n");
			break;
		case 3:
			printf("30点台です\n");
			break;
		case 4:
			printf("40点台です\n");
			break;
		}
	}
	else
	{
		printf("era-");
	}
}