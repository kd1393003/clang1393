#include<stdio.h>
main()
{
	int a, b, c, sum;
	float avg;
	printf("®”‚ğ3ŒÂ“ü—ÍF");
		scanf("%d%d%d", &a, &b, &c);
		sum = a + b + c;
			avg = (float)sum / 3;
		printf(" ‡Œv‚Í%d\t •½‹Ï=%.2f\n", sum, avg);
}